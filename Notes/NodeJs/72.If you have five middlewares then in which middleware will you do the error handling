If you have five middlewares then in which middleware will you do the error handling ? 
========================================================================
→ In case of multiple middlewares, error handling middleware should be defined at last because when an error  occurs, Express js will search for the  next error-handling middleware or  route handlers