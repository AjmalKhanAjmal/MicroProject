How to create an http server using Express js?
======================================
With Node : 
const http = require('http');
const server = http.createServer((req, res) => {    // Creating a server
  res.writeHead(200, { 'Content-Type': 'text/plain' });
  res.end('Hello from Node.js server');});
server.listen(3000, () => {  console.log('Server is running on port 3000');});

With Express : 
const express = require('express');
const app = express();     // Creating a server similar as http.createServer()
app.get('/', (req, res) => {
  res.send('Hello from Express server');
});
app.listen(3000, () => { console.log('Express server is running on port 3000'); });

How do you create and start an Express js application ?
===================================================
→ use above code
